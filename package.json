{
  "name": "ravm",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "webpack --watch",
    "browser": "cd ./dist && web-ext run --url https://transparent-vm.net:8080 --bc",
    "http": "cd ./demo-site && http-server -C cert.pem  -K key.pem -S",
    "lint": "npx eslint src",
    "lint:fix": "npx eslint --fix src",
    "build": "webpack build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "html-webpack-plugin": "^5.5.0",
    "webpack": "^5.76.0",
    "webpack-cli": "^4.10.0"
  },
  "dependencies": {
    "asn1js": "^3.0.5",
    "install": "^0.13.0",
    "node-forge": "^1.3.1",
    "pkijs": "^3.0.5"
  }
}
